define(['react', 'lodash', './styledText.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'styledText',
        render: template
    });
});
